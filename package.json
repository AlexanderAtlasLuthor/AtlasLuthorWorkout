{
  "name": "atlas-luthor-workout",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "deploy:cloudflare": "npm run build && wrangler pages deploy dist --project-name atlas-luthor-workout"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "wrangler": "^4.93.1"
  }
}
